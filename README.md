# Genshin Autopilot Build Repository

GitHub Actions build host for the Android Genshin Autopilot prototype.

The workflow decodes the checked-in source archive, runs unit tests, builds the debug APK, and uploads `app-debug.apk` as a GitHub Actions artifact.
